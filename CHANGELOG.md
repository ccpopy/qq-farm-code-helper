# 更新日志

## v0.2.2

### 可严格比较的登录握手归档

- 协议模式现在为每条 WebSocket 连接原样保存完整握手 URL（含明文 Code），并生成独立的 `connections.jsonl` 连接清单。
- 每条二进制消息的 `manifest.jsonl` 记录新增 `connection_id`，可把重新登录前后的 URL、登录回包与后续心跳严格关联到同一条连接。
- 界面、会话元数据和文档均明确提示握手 URL 含敏感登录凭据；资料只保存在本机，不写入普通日志，也不会上传。

## v0.2.1

### 协议监听与系统代理恢复加固

- 在本地代理端口右侧新增默认关闭的“协议模式”，用于新活动协议审查；该模式只保存完整的双向 WebSocket 二进制消息，不提取、保存或上传一次性 Code，也不会调用远程服务器接口。
- 抓包会话按全局收发顺序生成消息文件和清单，支持 WebSocket 拆帧、掩码与消息分片处理，便于按真实交互顺序回放和分析。
- 系统代理启停增加连续读回校验，保留原有 `ProxyOverride`、PAC 等配置；恢复失败时保留备份并明确报错，避免界面显示成功但系统代理未实际切换或恢复。

### 验证

- 通过 73 项 Rust 测试，7 项依赖外部环境或显式授权的测试保持忽略。
- 通过本机系统代理完整往返测试：切换至监听端口、稳定保持后恢复，并逐字段匹配切换前状态。
- 通过 Clippy 严格检查、rustfmt 格式检查及 Windows Release 构建。

## v0.2.0

### 多开 QQ 账号识别与切换

- 新增本机登录记录：助手会把在 `login.enc` 中观察到的账号（QQ 号、昵称、头像）持久化到程序目录的 `qq-login-history.json`，最多保留最近 16 个。
- 部分版本的 Windows QQ 多开时 `login.enc` 只保留最近一次登录，导致其余账号无法识别与切换；现在检测使用“当前登录信息 ∪ 历史记录”补齐仍在登录的账号。
- 安全模型不变：仅 QQ 主界面当前可见的账号会被确认，历史中的陈旧账号不会被自动绑定，捕获 Code 后仍会再次复核身份。
- 无匹配账号时的提示会引导重新登录。升级后首次使用请保持助手运行，并把各多开账号重新登录一次；此后即可直接在右侧账号卡片中选择本次进入农场的账号。

### 验证

- 通过 64 项 Rust 测试（新增登录记录持久化、覆盖式 login.enc 合并补齐、昵称刷新、容量淘汰及损坏文件容错等 6 项），6 项依赖本机或网络环境的测试保持忽略。
- 通过 Clippy 严格检查与 rustfmt 格式检查；实机确认多开双窗口昵称读取与新引导提示按预期工作。

## v0.1.9

### 兼容不同结构的 login.enc 登录列表

- 兼容 Windows QQ 将 `auth/login.enc` 写成顶层账号数组的结构，修复已登录 QQ 仍提示“Windows QQ 登录列表为空，请先登录 QQ”的问题。
- 原有 `loginList` 包装对象结构继续支持，两种格式的账号字段解析保持一致。
- 新增顶层数组格式的单元测试，以及解析本机真实 `login.enc` 的忽略测试，便于后续 QQ 格式变化时快速定位。

### 验证

- 通过 58 项 Rust 测试，6 项依赖本机或网络环境的测试保持忽略；实机确认真实 `login.enc` 成功解析出已登录账号。
- 通过 Clippy 严格检查与 rustfmt 格式检查。

## v0.1.8

### Windows QQ 多账号与好友同步修复

- 支持同时登录多个 Windows QQ：单账号自动确认，多账号可直接在右侧账号卡片中选择本次进入农场的账号，并在捕获完成后再次复核身份。
- 修复停止并恢复网络时重复执行清理、重复弹出证书删除提示及 PowerShell 错误信息显示异常的问题。
- 官方好友同步会在 Helper 本地排除当前农场账号自身 GID，只上传真实好友；同时清理远程历史缓存中曾保存的自身 GID。

## v0.1.7

### 官方登录后合并同步 Code 与好友 GID

- “同步官方好友”开启时先透明完成官方 WebSocket 登录，从官方 `SyncAll` 响应提取好友 GID，再同步 Code。
- 客户端未主动触发 `SyncAll` 时，Helper 会通过当前认证连接进行一次受控补发；空结果和超时均明确降级。
- Code 同步后使用服务器 Token 调用现有批量新增 GID 接口，无需修改 `qq-farm-bot` 的服务端协议。
- 不恢复协议观察入口，不记录或上传好友姓名、头像及 OpenID；好友捕获失败仍安全降级为仅同步 Code。

### 验证

- 实机确认官方登录正常完成，成功读取 20 位好友 GID；同一 Code 随后可启动远程账号，本地按预期收到同账号登录提示。
- 通过 49 项 Rust 测试，5 项依赖本机或网络环境的测试保持忽略。
- 通过 Clippy 严格检查、前端 JavaScript 语法检查、页面元素引用检查及 Release 构建。

## v0.1.6

### 同步官方 QQ 好友

- 新增“同步官方好友”流程：透传本次官方登录，并从成功的 `FriendService.SyncAll` 响应中提取、去重好友 OpenID 后同步到远程账号。
- 同步前校验本机 QQ 与远程账号归属，且要求目标账号正在运行；账号不匹配、尚未创建或已停止时不会上传数据，也不会自动创建账号。
- 好友 OpenID 仅保存在内存和本次同步请求中，不写入本地文件或日志；好友流程不会向远程上传一次性 Code。
- 移除协议观察模式及通用 RPC 记录，保留原有 Code 获取、远程同步和自动启动账号流程。
- 需要远程 `qq-farm-bot` 支持 `POST /api/friends/sync-open-ids` 接口；未使用好友同步功能时不影响原有逻辑。

### 验证

- 通过 48 项 Rust 测试，5 项依赖本机或网络环境的测试保持忽略。
- 通过 Clippy 严格检查、前端 JavaScript 语法检查及页面元素引用检查。

## v0.1.5

### GitHub Release 自动更新

- 启动后自动检查最新稳定版 GitHub Release，也支持手动检查。
- 默认通过 `https://gh.lessdo.top` 加速下载，用户可关闭后直接连接 GitHub。
- 安装版会静默安装到当前程序目录；便携版会原地替换当前 EXE，不会改回默认 C 盘路径。
- 更新前校验 GitHub API 提供的官方 SHA-256，校验失败时拒绝执行下载文件。
- 安装前自动退出当前程序并停止同路径残留进程，完成后重新启动应用。

### 验证

- 新增 Release 版本选择、代理地址、SHA-256、设置迁移、安装目录保留及便携版原地替换测试。
- 通过完整 Rust 测试、格式检查、Clippy、前端语法检查和 PowerShell 更新脚本解析。
- 实测 GitHub Release API、`gh.lessdo.top` 代理下载及关闭代理后的 GitHub 直连下载。

## v0.1.4

### 同步后自动启动

- Code 成功同步到远程服务器并取得账号 ID 后，自动调用 `POST /api/accounts/{id}/start`。
- 复用同一 QQ 的原账号时，已停止账号无需再前往管理页面手动启动。
- 自动启动后继续等待远程运行状态及游戏身份回填，界面能够显示最新运行状态。
- 若 Code 已保存但启动接口调用失败，会给出明确的分阶段错误提示。

### 验证

- 新增首次同步自动启动、原账号恢复运行及启动失败提示回归测试。
- 通过完整 Rust 测试、格式检查和 Clippy 静态检查。

## v0.1.3

### 账号同步修复

- 同一服务器用户下，相同 QQ 号再次获取 Code 时复用原账号 ID，不再重复创建账号。
- 更新现有账号时保留服务器上的账号备注，以及绑定在原账号 ID 下的策略和用户配置。
- 管理员账号兼容旧版本中尚未绑定用户名的历史账号。
- 已存在多个相同 QQ 账号时，优先复用最早创建的账号，以尽可能保留原有配置。
- 不会覆盖其他服务器用户拥有的同 QQ 账号。

### 验证

- 新增首次同步、同 QQ 更新、跨用户隔离及旧账号兼容回归测试。
- 通过完整 Rust 测试和 Clippy 静态检查。
